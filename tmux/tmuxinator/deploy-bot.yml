# ~/.tmuxinator/deploy-bot.yml

name: deploy-bot
root: ~/molecule/deploy-bot

windows:
  - deploy-bot-vim:
      layout: main-vertical
      panes:
        - vim .
  - deploy-bot-cli:
      layout: main-vertical
